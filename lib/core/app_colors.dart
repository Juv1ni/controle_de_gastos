import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bg = Color(0xFFF6F7F9);
  static const card = Colors.white;
  static const text = Color(0xFF0F172A);
  static const subtext = Color(0xFF64748B);
  static const border = Color(0xFFE6E8EE);

  // Accents (sutis)
  static const positive = Color(0xFF16A34A); // verde suave
  static const negative = Color(0xFFDC2626); // vermelho suave
  static const neutralIcon = Color(0xFF111827);
}
